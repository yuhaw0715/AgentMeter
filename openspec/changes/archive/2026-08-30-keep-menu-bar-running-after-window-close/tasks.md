## 1. 應用程式生命週期修正

- [x] 1.1 在 `AppDelegate` 明確實作最後一個主視窗關閉後不終止應用程式的政策
- [x] 1.2 確認紅色關閉按鈕與 `Cmd-W` 只關閉主視窗、隱藏 Dock 執行中狀態，且不影響 Popover Quit、系統 Quit 與 `Cmd-Q` 的完整終止流程
- [x] 1.3 確認關閉主視窗後，可由 Menu Bar 重新開啟單一主視窗，恢復 Dock 顯示且不產生重複視窗

## 2. 測試與驗收

- [x] 2.1 新增應用程式終止政策的自動化回歸測試，驗證最後視窗關閉時回傳不終止並切換為 `.accessory`
- [x] 2.2 執行完整 Swift 測試套件與 release build，確認既有功能未回歸
- [x] 2.3 使用建置後 App 手動驗收紅色關閉、`Cmd-W`、Dock 執行中狀態消失、Menu Bar 常駐、主視窗重新開啟及 `Cmd-Q`／Quit 終止流程
- [x] 2.4 將驗證結果與修改摘要記錄於實作結案總結（Walkthrough）
